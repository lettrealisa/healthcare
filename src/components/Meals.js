import useStorage from "../hooks/useStorage";
//import useAxios from "../hooks/useAxios";

const Meals = () => {
    /* useStorage(); */
    //const { getConn } = useAxios();
    //getConn();
    return (
        <><h1>Дневник питания</h1></>
    );
}

export default Meals